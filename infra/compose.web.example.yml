services:
  praxis-web-build:
    build:
      context: ..
      dockerfile: infra/Dockerfile.web
      args:
        SITE_URL: ${PRAXIS_SITE_URL:-https://praxis.example}
    volumes:
      - praxis-web-dist:/output
    restart: 'no'

volumes:
  praxis-web-dist:
