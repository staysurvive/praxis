---
import { uiCopy } from '../config/copy';
import { journeyKeys } from '../lib/content/domain';
import type { Journey } from '../lib/content/schema';

interface Props {
  journey?: Journey;
}

const { journey } = Astro.props;
const sections = journey
  ? journeyKeys.flatMap((key) => {
      const value = journey[key];
      return value ? [{ key, label: uiCopy.journey[key], value }] : [];
    })
  : [];
---

{
  sections.length > 0 && (
    <section class="journey-panel rule-scotch" aria-labelledby="journey-title">
      <div class="journey-intro">
        <p class="eyebrow">{uiCopy.journeyPanel.eyebrow}</p>
        <h2 id="journey-title">{uiCopy.journeyPanel.title}</h2>
        <p>{uiCopy.journeyPanel.description}</p>
      </div>
      <div class="journey-list">
        {sections.map((section, index) => (
          <article class="journey-item">
            <span class="journey-index" aria-hidden="true">
              {String(index + 1).padStart(2, '0')}
            </span>
            <div>
              <h3>{section.label}</h3>
              <p>{section.value}</p>
            </div>
          </article>
        ))}
      </div>
    </section>
  )
}

<style>
  .journey-panel {
    display: grid;
    gap: clamp(2.5rem, 6vw, 5rem);
    padding-block: clamp(4rem, 8vw, 7rem);
  }

  .journey-intro {
    max-width: 34rem;
  }

  .journey-intro h2,
  .journey-intro p {
    margin: 0;
  }

  .journey-intro h2 {
    margin-top: 1rem;
    font-family: var(--font-serif);
    font-size: var(--text-title-1);
    font-weight: 400;
    letter-spacing: var(--track-cjk-display);
  }

  .journey-intro > p:last-child {
    margin-top: 1rem;
    color: var(--color-text-muted);
  }

  .journey-list {
    border-top: 1px solid var(--color-border);
  }

  .journey-item {
    display: grid;
    grid-template-columns: 3.5rem minmax(0, 1fr);
    gap: 1rem;
    padding-block: 1.7rem;
    border-bottom: 1px solid var(--color-border);
  }

  .journey-index {
    color: var(--color-accent);
    font-family: var(--font-serif);
    font-size: 0.875rem;
    letter-spacing: var(--track-meta);
  }

  .journey-item h3,
  .journey-item p {
    margin: 0;
  }

  .journey-item h3 {
    color: var(--color-text);
    font-size: var(--text-meta);
    font-weight: 500;
    letter-spacing: var(--track-meta);
  }

  .journey-item p {
    max-width: 44rem;
    margin-top: 0.65rem;
    color: var(--color-text-muted);
    line-height: 1.9;
  }

  @media (min-width: 56rem) {
    .journey-panel {
      grid-template-columns: minmax(16rem, 0.7fr) minmax(0, 1.3fr);
    }
  }
</style>
